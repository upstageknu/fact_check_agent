env:
  required:
    - ORCHESTRATOR_BASE_URL
    - UPSTAGE_API_KEY
  optional:
    - DATABASE_URL
    - REPO_PATH
    - REPO_URL
    - UPSTAGE_BASE_URL
    - SOLAR_MODEL

name: fact_check

endpoints:
  health: GET /health
  invoke: POST /invoke

port: 8000
